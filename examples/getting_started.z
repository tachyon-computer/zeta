use libc(printf)

func main() -> int
{
    let A: double[5] = {
        [0] = 9.0,
        [1] = 2.9,
        [4] = 3.E+25,
        [3] = .00007
    };

    for(i: int = 0; i < 5; ++i)
    {
        printf("element %zu is %g, \tits square is %g\n", i, A[i], A[i]*A[i]);
    }

    return 0;
}
